const productStatusValues = Object.freeze([
    'active',
    'unactive',
    'draft'
])

const bulkUpdatableProductStatuses = Object.freeze([
    'active',
    'unactive',
    'draft'
])

module.exports = {
    bulkUpdatableProductStatuses,
    productStatusValues
}
