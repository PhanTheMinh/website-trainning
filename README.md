# website-trainning
Trainning NodeJS for newbie

## Frontend Vue.js

Project frontend nam trong thu muc `frontend/`.

Chay frontend:

```bash
cd frontend
npm install
npm run dev
```

Cau hinh backend URL tai `frontend/.env.development`:

```env
VITE_API_BASE_URL=http://localhost:3000
```
